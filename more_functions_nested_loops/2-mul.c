#include "main.h"

/**
 * mul(int a, int b) - function that multiplies two integer.
 * @a - first number
 * @b - second number
 * Return: return result
 */

int mul(int a, int b)
{
	return (a * b);
}
